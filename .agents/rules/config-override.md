---
trigger: always_on
---

Project config for commit workflow:
VariableValue{lint_command}npm run lint{build_command}npm run <build@dev_instructionsdev_instruction_v3.1.md>@project_configHeroUI v3 MCP, ui-tokens.ts, index.css
Additional project-specific rules:

Use HeroUI v3 MCP to verify all component and pattern compliance
All styling must reference ui-tokens.ts — no hardcoded values
index.css is the source of truth for all CSS variables and theme tokens
