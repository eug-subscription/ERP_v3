// Shared validation utilities — import these instead of defining inline.

// Basic email format check — validates structure, not deliverability.
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
