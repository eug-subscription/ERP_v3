// Margin health thresholds (% of revenue)
// Good: ≥30% — displayed as success
// Acceptable: ≥10% — displayed as warning
// Poor: <10% — displayed as danger
export const MARGIN_THRESHOLD_GOOD = 30;
export const MARGIN_THRESHOLD_ACCEPTABLE = 10;

// Currency fallback when no billing lines exist
export const DEFAULT_CURRENCY = 'EUR';
