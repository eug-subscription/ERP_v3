/**
 * Shared constants for TanStack Query hooks.
 */

// Default stale time for pricing-related data (5 minutes)
export const DEFAULT_STALE_TIME = 1000 * 60 * 5;

// Default delay for mock API calls
export const MOCK_API_DELAY = 800;

